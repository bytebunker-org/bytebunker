import { SetMetadata } from '@nestjs/common';
//import { UserRoleEnum } from '../../user/type/user-role.enum.js';

export const ROLE_DECORATOR_KEY = 'role';
//export const Role = (role: UserRoleEnum) => SetMetadata(ROLE_DECORATOR_KEY, role);
